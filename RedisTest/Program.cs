namespace RedisTest
{
    class Program
    {
        
        static void Main3(string[] args)
        {
            //Usando cliente Stack Exchange

            //Console.WriteLine("Acabou, Tempo");
            //KeyValue keyValue = new KeyValue();
            //SerializationJson serialization = new SerializationJson(keyValue);

            //Stopwatch stopWatch = new Stopwatch();
            //stopWatch.Start();
            //serialization.ConvertString();
            //stopWatch.Stop();
            //Console.WriteLine("Acabou, Tempo: {0:hh\\:mm\\:ss}", stopWatch.Elapsed);
            //var user = new User();
            /*Nosso servidor
            *C:\ProgramData\Chocolatey\lib\redis-64
            */
            //Conexão com o servidor, podemos passar também o user e senha
            //ConnectionMultiplexer ConnectRedis = ConnectionMultiplexer.Connect("localhost:6379");

            //Cliente Redis responsavel por enviar comandos.
            //IDatabase clienteRedis = ConnectRedis.GetDatabase();

            //Stopwatch stopWatch = new Stopwatch();
            //stopWatch.Start();
            //KeyValue keyValue = new KeyValue();
            //stopWatch.Stop();

            //List<string> chavesListCliente = keyValue.GeraChaveCliente();

            //List<Dictionary<string, string>> camposClienteList = keyValue.GeraCamposCliente();
            //IList<Locadora.Cliente> clientes = keyValue.GetClientes;

            //List<Dictionary<string, string>> camposFilmeList = keyValue.GeraCamposFilmes();
            //IList<Locadora.Filme> filmes = keyValue.GetFilmes;

            //List<Dictionary<string, string>> camposCategoriaList = keyValue.GeraCamposCategorias();
            //IList<Locadora.Categoria> categorias = keyValue.GetCategorias;

            //foreach (var item in categorias)
            //{
            //    Console.WriteLine("Cod Categoria {0} | Descricao Categoria {1} | Valor Categoria {2}",
            //        item.CodCategoria, item.DescricaoCategoria, item.ValorCategoria);
            //}
            //IList<Locadora.Locacao> locacaoList = keyValue.GetLocacoes;
            //List<string> chavesLocacaoList = keyValue.GeraChaveLocacao();
            //List<Dictionary<string, string>> camposLocacaoList = keyValue.GeraCamposLocacao();


            //Console.WriteLine("Criação de Objetos: {0:hh\\:mm\\:ss}", stopWatch.Elapsed);
            //Console.ReadKey();

            //foreach (var dictionary in camposLocacaoList)
            //{
            //    foreach (var campoValor in dictionary)
            //    {
            //        Console.WriteLine("{0} {1}", campoValor.Key, campoValor.Value);
            //    }
            //}

            //stopWatch.Start();
            //int i = 0;
            //foreach (var dictionary in camposList)
            //{
            //    foreach (var campoValor in dictionary)
            //    {
            //        clienteRedis.HashSet(chavesList[i], campoValor.Key.ToUtf8Bytes(), campoValor.Value.ToUtf8Bytes());
            //    }
            //    i++;
            //}
            //stopWatch.Stop();
            //Console.WriteLine("Inserção no Redis: {0:hh\\:mm\\:ss}", stopWatch.Elapsed);


            //Close connection
            //ConnectRedis.Close();

            //Console.ReadKey();
        }

    }
}
